server {
    listen 80;
    server_name akunku.mykisah.id;

    # Redirect HTTP → HTTPS (aktifkan setelah SSL terpasang)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:3008;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Aktifkan blok ini setelah install certbot (Let's Encrypt)
# server {
#     listen 443 ssl;
#     server_name akunku.mykisah.id;
#
#     ssl_certificate /etc/letsencrypt/live/akunku.mykisah.id/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/akunku.mykisah.id/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:3008;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
